# Task 0010 - CLI Foundation

Status: Done
Priority: P0

## Goal

Implement the MVP CLI.

## Commands

- omni search
- omni install
- omni remove
- omni explain

## Acceptance Criteria

- commands call core services
- no business logic duplicated in CLI layer
- tests exist

## References

- specs/08-CLI_SPEC.md

## Completion Notes

- Fixed `Application.ID` field reference (was incorrectly `ApplicationID`) in `cmd/omniinstall/app.go`.
- `cmd/omniinstall/app.go` wires discovery, resolver, engine, and state into the `App` struct; all five commands (`search`, `install`, `remove`, `explain`, `list`) delegate to core services with no duplicated business logic.
- `cmd/omniinstall/app_test.go` covers: search found/not-found/empty-query, install success/not-found/empty/no-compatible-source, remove success/empty-id, explain found/not-found/empty, list empty/with-installations/hides-removed.
- All 13 packages pass `go test ./...`; build succeeds with `go build ./...`.
